# Set to true to replace documentation pages with the shared maintenance notice.
MAINTENANCE_MODE=false

# Badge service host — override for local development
# NEXT_PUBLIC_BADGE_HOST=http://localhost:8787
