---
"docs": patch
---

Fix SEO and metadata: replace Organization JSON-LD schema with WebSite, add viewport export with gold theme colour, add keywords/category/creator, update PWA manifest (name, start_url, icon purposes), use baseUrl constant throughout, enhance TechArticle schema with author/publisher, explicit OG metadata, and custom branded OG image.
