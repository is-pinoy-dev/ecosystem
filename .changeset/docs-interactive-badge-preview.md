---
"docs": patch
---

Add a live, interactive preview of the `<is-pinoy-badge>` web component to the badge-kit docs page. Visitors can switch type, theme, and shimmer mode, toggle the tilt, and hover the rendered badge to feel the shimmer + ID-card tilt — with the matching embed snippet generated live below it.
